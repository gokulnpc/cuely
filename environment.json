{
  "install": "npm ci && npm ls electron concurrently wait-on cross-env tsx --depth=0",
  "terminals": [
    {
      "name": "checks",
      "command": "npm run typecheck && npm run lint && npm run test"
    }
  ]
}
